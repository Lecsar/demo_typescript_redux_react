import { makeStyles } from '@material-ui/styles';

export const withRouterStyles = makeStyles({
  toastContainer: {
    fontSize: '1.7rem',
    fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif'
  }
});
